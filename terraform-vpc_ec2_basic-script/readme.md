## Configure AWS access and secret key in CMD Windows
```bash
set AWS_ACCESS_KEY_ID=AKIAxxxxxxxx
set AWS_SECRET_ACCESS_KEY=xxxxxxxxxxxxxxxxx
```

## Check AWS key using AWS CLI
```bash
aws configure list
```

## Terraform command
```bash
terraform init
terraform fmt
terraform validate
terraform plan
terraform apply
```